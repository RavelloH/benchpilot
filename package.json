{
  "name": "benchpilot",
  "version": "0.0.0",
  "description": "Agent-first embedded device lifecycle toolkit.",
  "license": "MIT",
  "author": "RavelloH",
  "type": "module",
  "packageManager": "pnpm@11.1.3",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "bin": {
    "benchpilot": "./dist/cli/index.js"
  },
  "engines": {
    "node": ">=22.13"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/RavelloH/benchpilot.git"
  },
  "bugs": {
    "url": "https://github.com/RavelloH/benchpilot/issues"
  },
  "homepage": "https://github.com/RavelloH/benchpilot#readme",
  "keywords": [
    "embedded",
    "firmware",
    "automation",
    "device-management",
    "agent",
    "cli",
    "hardware",
    "microcontroller"
  ],
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "docs"
  ],
  "scripts": {
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "build": "pnpm run adapter:validate && tsc -p tsconfig.json && pnpm run adapter:compile",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && node --test test/**/*.test.js",
    "test:coverage": "pnpm run build && node --test --experimental-test-coverage test/**/*.test.js",
    "test:package": "pnpm run test:package:npm",
    "test:package:npm": "pnpm run build && node test/package-e2e.mjs",
    "adapter:validate": "tsx src/adapters/compiler/cli.ts validate",
    "adapter:compile": "tsx src/adapters/compiler/cli.ts compile",
    "adapter:test": "tsx src/adapters/compiler/cli.ts test",
    "pack:check": "pnpm pack --dry-run",
    "format": "prettier --write . --log-level warn",
    "format:check": "prettier --check . --log-level warn",
    "prepare": "husky"
  },
  "dependencies": {
    "@iarna/toml": "^2.2.5",
    "rlog-js": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.8.3",
    "tsx": "^4.19.2",
    "typescript": "^5.6.0"
  },
  "lint-staged": {
    "*.{js,mjs,cjs,ts,tsx,json,yml,yaml,md}": "prettier --write --ignore-unknown"
  }
}
