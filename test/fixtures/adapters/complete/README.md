# Complete Fixture

This is the Format v1 conformance fixture, not a builtin adapter. It exercises
direct and via-tool launches; discovery and environment providers; device
matching; process, serial and copy actions; parser extraction, progress and
errors; path and glob artifacts; all three platform overlays; and every
declaration case type. Tests validate, compile and run its declaration cases.
