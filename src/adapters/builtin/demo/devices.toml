schema = "benchpilot.adapter.devices"
schema_version = 1
[discovery]
enabled = false
[identity]
fields = ["device.device_id"]
allow_port_fallback = false
[probe]
enabled = false
reason = "The simulated adapter has no physical probe."
