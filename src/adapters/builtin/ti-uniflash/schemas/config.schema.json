{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "benchpilot://adapter/ti-uniflash/config",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "python_path": {
      "type": "string",
      "minLength": 1,
      "description": "Optional Python executable with the pyserial module used by bounded UART capture."
    },
    "dslite_path": {
      "type": "string",
      "minLength": 1,
      "description": "Full path to the DSLite executable, not the dslite.bat wrapper."
    },
    "make_path": {
      "type": "string",
      "minLength": 1,
      "description": "Optional Make executable used by the declared Make build backend."
    },
    "cmake_path": {
      "type": "string",
      "minLength": 1,
      "description": "Optional CMake executable used by the declared CMake build backend."
    }
  }
}
